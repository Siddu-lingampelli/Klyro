- [INFO] src/agent/* and src/tools/** coverage start
- [HIGH] src/agent/runtime.ts:602-716 - Tool execution is forced sequential via or (const call of finalizedCalls); llSafe/isConcurrencySafe flag is computed but unused; advertised parallel execution is dead
- [HIGH] src/agent/runtime.ts:402-413 - tool args JSON parse failure is wrapped to {_parse_error:true} then forwarded to the registry, causing the tool to run with garbage input; should return an error tool_result instead
- [HIGH] src/agent/runtime.ts:557-578 - Repair loop emits repair context but epairs++ is set above the guard check; guardRepair blocks are pushed as transcript text but the loop continues without exit; verify_failed -> no limit on attempts inside step
- [HIGH] src/agent/runtime.ts:707-715 - stuckCount (identical call x3) and ileEditCounts (>8 edits same file) only emit telemetry + a user note, never terminate or block; loops can burn full maxSteps repeating the same broken call
- [HIGH] src/agent/runtime.ts:784-792 - inferFileChanged returns null for multi_edit, pply_patch, and shell; those files are missing from ileEditCounts/scoped verification/edited set even when they made real changes
- [MED] src/agent/runtime.ts:651-652 - approval "edit" path's editedInput is unused; original call input is executed verbatim regardless of user edits; also epairs++ conflates approval path with verify repair attempts
- [MED] src/agent/runtime.ts:170 - /o1/i rate regex matches any model with 'o1' substring (incl. unknown providers); gpt-4o-mini falls into gpt-4 default; cost estimate may be off by an order of magnitude
- [MED] src/agent/runtime.ts:737-738 - when opts.signal?.aborted is checked only at loop top and after final-text branches; mid-stream abort during tool exec is not observed and may overrun signal
- [MED] src/agent/runtime.ts:293-299 - first wait checkpoint(msg) after building transcript is not guarded by fire-and-forget but later checkpoints are awaited; inconsistent persistence ordering
- [MED] src/agent/runtime.ts:516-531 - pre-existing failure treated as non-blocking but only checks ailurePaths overlap with introducedPaths (basenames); ignores other regressions introduced elsewhere
- [MED] src/agent/runtime.ts:741-746 - maxSteps exit path emits inal_text then sets status max_steps without a verification result when hasEdits is true; tail-latency hook for verification missing
- [LOW] src/agent/runtime.ts:187-194 - cachedTotalTokens memo uses reference equality; pushing to transcript mutates its identity (no, transcripts are re-assigned) — actually fine but tightly coupled to upstream not mutating initialTranscript
- [LOW] src/agent/runtime.ts:267-289 - checkpoint uses dynamic import for store path; if first call fails it is logged but swallowed, no retry on later steps
- [LOW] src/agent/runtime.ts:577 - telemetry.recordError uses string with verb; recorded as a single code, may overflow rate counters when erify_* is the prefix
- [HIGH] src/tools/shell/shell-exec.ts:91-97 - command-substitution regex / /, /(/) and |bash/|sh/ would block many legitimate shell uses (echo 09/06/2026 14:22:41, cat file | head); only sane if --yolo is the override; otherwise every build command with subshells is rejected
- [HIGH] src/tools/shell/shell-exec.ts:151-158 - spawn uses shell: true so user-supplied command line is fully interpreted by /bin/sh -c on Unix and cmd.exe /c on Windows; persistent cwd is honored but env filtering can be circumvented via inline NODE_OPTIONS= because filteredEnv runs after merging extra — wait, NODE_OPTIONS is explicitly stripped; check next
- [HIGH] src/tools/shell/shell-exec.ts:53-62 - filteredEnv uses ALLOWED_ENV_PREFIXES allowlist but extra is iterated with or k of extra and PATH/PWD/TMPDIR/TEMP may be overwritten by user-supplied values bypassing policy (PATH can point to a malicious bin dir)
- [HIGH] src/tools/shell/shell-exec.ts:151-158 - signal: ctx.signal passed to spawn but on error finish resolves with {code:null, signal:null, timedOut:false} even when abort caused the spawn error; abort is mis-classified as exitCode=null and tool returns success shape with no indication
- [HIGH] src/tools/shell/shell-exec.ts:202-215 - on POSIX timeout the timer issues SIGKILL but only to the direct child; the entire process group inherits stdin/stdout but not signal forwarding; SIGKILL leaves grandchildren running (same Windows tree-kill problem inverted)
- [MED] src/tools/shell/shell-exec.ts:224-232 - full output written to ~/.klyro/tool-output/<id>.txt without atomic rename; partial files can be read by parallel jobs list
- [MED] src/tools/shell/shell-exec.ts:170-188 - truncation logic stores only the first 10 chunks once truncated=true, so stdout returned to model only has the first ~10 chunks of head and never any tail; comment says head+tail but only head is kept
- [MED] src/tools/shell/shell-exec.ts:235-244 - headTail uses 15k head + 15k tail but stdoutChunks already capped to 10 chunks (each may be 64k); truncated output loses intermediate content but 	runcated flag is set on first over-budget chunk, not at end of stream
- [MED] src/tools/shell/shell-exec.ts:34-50 - persistentCwd is module-global mutable state; concurrent shells (different sessions, /abort) clobber each other; per-context cwd is preferred
- [HIGH] src/tools/shell/background.ts:34-45 - startBackground does NOT pass signal/env filter, does NOT verify command against DANGEROUS_PATTERNS, does NOT check INTERACTIVE_PATTERNS — g command is a full shell_exec bypass
- [HIGH] src/tools/shell/background.ts:22-31 - pruneJobs only kills when size>MAX_JOBS but never calls pruneJobs on TTL expiry except inside the >MAX branch; long-running idle jobs linger until replaced
- [MED] src/tools/shell/background.ts:40-42 - output buffering: stdout and stderr are appended to the same output string; interleaving is lost; no truncation beyond 1MB ring
- [MED] src/tools/shell/background.ts:43-44 - setTimeout to delete closed jobs uses raw ms math but proc.exitCode !== null check happens inside pruneJobs only — not on the timer itself
- [HIGH] src/tools/fs/write-file.ts:39-67 - esolveAndFollowSymlinks is called but the tmp file is opened at path.join(parent, '.klyro-write-<pid>-<hex>.tmp') AFTER mkdir; parent is the same dir as esolved so symlink-following during mkdir may create dirs outside policy; mkdir uses ecursive: true (no-op) but the resulting 	mp path is unconstrained vs esolved
- [HIGH] src/tools/fs/write-file.ts:46-53 - TOCTOU between eadFile (existence check) and ename; an attacker able to write to cwd can swap the target for a symlink between read and rename (resolved was followed earlier but final rename may resolve symlinks afresh)
- [MED] src/tools/fs/write-file.ts:67-71 - on rename failure, unlink(tmp) is best-effort; if unlink fails, the tmp file persists in workspace (visible via glob/grep/recent_files)
- [MED] src/tools/fs/write-file.ts:74-78 - diff is computed from first 20 lines only; large file overwrites show truncated diff in approval UI
- [HIGH] src/tools/fs/edit-file.ts:43-62 - stat-check wasRead may be stale across cwd/s.rename swap; same TOCTOU window as write_file
- [HIGH] src/tools/fs/edit-file.ts:99-122 - 'fuzzy tiers' recompute on every edit; if file has been mutated between read and edit but content unchanged on disk the stale check changed externally may false-pass; comment says 'checks staleness' but mechanism is by-mtime not by-content-hash
- [HIGH] src/tools/fs/edit-file.ts:127-132 - 'ambiguous matches fail' check counts ind.length only; substring overlapping (e.g. 'a' in 'aaa') is not detected as ambiguous; user may silently get unexpected replacement
- [MED] src/tools/fs/list-dir.ts - read; SKIP_DIRS list includes 'node_modules','.git' but does NOT skip symlinks (uses fs.readdir without lstat filter); a symlink to /etc inside cwd would be traversed
- [HIGH] src/tools/fs/apply-patch.ts:33-67 - uses diff-match-patch; reads whole file; if path resolves outside cwd (after symlink follow) the patch is applied regardless — relies on caller to pass --yolo or policy check upstream
- [HIGH] src/tools/fs/multi-edit.ts - reads all target files; path validation uses esolveAndFollowSymlinks but if any file is missing the whole batch fails; atomicity only at API surface not filesystem
- [HIGH] src/tools/fs/read-file.ts:35-56 - resolveAndFollowSymlinks called BEFORE size cap; a symlink to /etc/passwd inside cwd is followed, then refused at size 10MB; but reads the first chunk during size check (uses fs.open + read loop) which may execute side-effects if file is special
- [MED] src/tools/fs/read-file.ts:111-115 - storeResult duplicates file content into context.lifecycle; with 1MB+ files the lifecycle map grows unbounded until clearLifecycle (which is never called in this tool)
- [LOW] src/tools/fs/read-history.ts - module-level Set per process; never cleared between sessions; multi-session in one process (eval harness) sees stale 'wasRead' hits
- [HIGH] src/tools/normalize.ts:60-62 - isToolErrorCode uses Object.values against exported constants; auto-adds MATCH_NOT_FOUND, MATCH_AMBIGUOUS, UNKNOWN_TOOL, INVALID_INPUT; if any tool throws a code like 'POLICY_DENIED' (runtime uses this string) it is not recognized as a tool error code and falls through to allbackCode=INTERNAL losing the code in the ToolResult
- [HIGH] src/tools/normalize.ts:96-103 - safe passes through esult.ok === false && result.error shape; VerifyOutput (run-verify) has ok: boolean for 'did verify succeed', not the harness ToolResult shape; this conflation means VerifyOutput.ok=false is treated as a tool error and reported via error.path in policy — bypasses the intended success-with-error semantics
- [MED] src/tools/registry.ts:69-87 - execute catches parse failures and returns UNKNOWN_TOOL/INVALID_INPUT but does NOT enforce isConcurrencySafe; runtime's own parallel path is dead anyway (see runtime.ts), but registry permits any tool to opt out — the per-tool flag has no enforcement layer
- [MED] src/tools/schema.ts:200-210 - ZodPipe/ZodTransform/ZodCatch defaults to 	ype:'object', additionalProperties:true; tools using z.coerce.number() or z.preprocess lose their type info in the JSON Schema sent to the model — model sends strings, validation fails, _parse_error loops in runtime
- [MED] src/tools/schema.ts:160-180 - ZodRecord with non-string keys is mis-described as object; ZodMap; ZodSet fall through to default object type
- [HIGH] src/tools/verify/run-verify.ts:54-72 - verify uses generic spawn without dangerous-pattern filter, filtered env, or path policy; the 'verify' permission bypass is intentional (comment) but should require explicit approval
- [HIGH] src/tools/verify/run-verify.ts:79-95 - on 'error' event finish resolves with timedOut=false and exitCode=null; identical misclassification as shell-exec; abort signal -> exitCode=null, exit_code reported as successful verify failure
- [HIGH] src/tools/git/git-diff.ts:58-66 - parses diff --git a/X b/Y lines but does not handle 'rename' or 'copy' diffs (diff --git a/X b/Y with similarity index), also handles binary files (GIT binary patch) by truncating to empty; results in patchedFiles containing the rename source instead of target
- [MED] src/tools/git/git-status.ts:23-36 - run() spawns git without windowsHide consistently across all three tools (git-status, git-diff, git-log differ); output collected as Buffer concat with no truncation — git log -p (which this tool never asks for) would explode; not currently a bug but tight coupling
- [MED] src/tools/git/git-log.ts:36-37 - rejects with raw stderr; no structured error code; runtime classifies as tool_error with code='tool_error' losing GIT context
- [HIGH] src/tools/search/grep.ts:38-50 - 'Pattern rejected (potential ReDoS)' is heuristic only; nested quantifiers (.+)+ are not detected; agents can craft DoS regex via tool input
- [HIGH] src/tools/search/grep.ts:120-140 - walk() is synchronous per-call (uses await per dir) but readdir + lstat + read is sequential; large repos take O(N) wall time; no concurrency control beyond the await loop
- [MED] src/tools/search/glob.ts:67-101 - walk() never breaks early even after out.length >= cap when current dir contains many subdirs; recursive descent can scan entire repo before stopping at cap (although the for-loop checks cap each iter)
- [MED] src/tools/search/recent-files.ts:51-66 - same walk pattern as glob; reads stat() for every file; no mtime-indexed fast path
- [MED] src/tools/search/dependencies.ts - read head+early; no dangerous-pattern checks; reads Cargo.toml/pyproject.toml which can be huge; MAX_BYTES cap of 64k may truncate real Cargo manifests
- [LOW] src/tools/search/imports.ts - thin wrapper over context/import-graph; no caching of importsOf results; called per request
- [MED] src/tools/lsp/diagnostics.ts - stub-only; returns {enabled:false} when KLYRO_LSP!=1 — agent wastes a turn discovering it is disabled
- [MED] src/tools/symbols/find-symbol.ts - regex buildRepoMap; full repo scan per call; no caching; description admits tool is disabled by default (decision 7.4)
- [HIGH] src/tools/plan/todo-write.ts:25-30 - writes JSON.stringify(input.todos) with no schema validation of the array contents beyond zod; no fsync; power-loss can leave half-written todos.json consumed by 5.3 logic downstream
- [MED] src/tools/plan/ask-user.ts:17-22 - nonInteractive + KLYRO_AUTO_ANSWER returns the same string for every question regardless of input.question; agent can issue 5 different asks and receive identical answers — trivial auto-answer bypass
- [HIGH] src/tools/repo-map.ts:30-46 - importInDegree uses cand.includes(resolved) || resolved.includes(cand.slice(0, 10)) — substring match produces false-positive edges; score inflation is acceptable but explains noise
- [HIGH] src/tools/repo-map.ts:78-81 - caps to 6000 chars but the cap is not applied to iles array (paths) which can be 40 entries * ~80 chars = 3.2k extra; combined output exceeds 9k chars not 6k
- [LOW] src/tools/memory-write.ts:11-14 - description says '≤1k tokens injected' but no actual truncation; long content will be injected in full by caller
- [LOW] src/tools/expand-result.ts - thin; relies on context/lifecycle which the audit did not cover
- [HIGH] src/agent/retry.ts:113-130 - on retryable error the loop sleeps delay = computeBackoff(attempt) even on the LAST attempt (attempt === maxAttempts-1) before yielding the error; wastes up to 8s; should only sleep if a subsequent attempt will happen
- [HIGH] src/agent/retry.ts:95-97 - abort listener: eason = sigs.find(s => s.aborted)?.reason but the retry wrapper itself creates a fresh AbortController; passing eason as Error into whole-module Abort constructor; semantics unclear
- [HIGH] src/agent/retry.ts:112-133 - retry policy applies to NETWORK/transport/stream errors but NOT to HTTP 5xx surfaced via HTTP_* error code; provider-adapter only sets retryable for 5xx/429 — but 4xx rate-limits (other providers) are never retried
- [MED] src/agent/retry.ts:23-40 - DEFAULT_RETRY_OPTIONS not exported; runtime must construct its own or accept adapter-internal defaults — observable drift between file doc and implementation
- [HIGH] src/agent/anthropic-adapter.ts:299-303 - drops tool-call deltas by design to avoid mis-routing; if a tool block is split across multiple content_block_delta chunks and the mapping is ambiguous, the input JSON is silently incomplete
- [MED] src/agent/anthropic-adapter.ts:70-71 - message.slice(0, 500) truncation of API error bodies may cut off key diagnostic codes; bodies often >500 chars with field paths
- [MED] src/agent/anthropic-adapter.ts:271-290 - yields 'error' on API error but does NOT check status codes for retryability — same HTTP classification missing as provider-adapter
- [MED] src/agent/anthropic-adapter.ts:337-338 - system messages hoisted to a top-level field but 'system' role messages inside the array are dropped with empty text; combined with indToolIdByIndex mapping empty content to {type:'text',text:''} the API may strip them anyway
- [MED] src/agent/anthropic-adapter.ts:86-87 - throws on missing fetch implementation synchronously; only error path, no graceful fallback
- [HIGH] src/agent/worker-spawner.ts:38-58 - spawn() registers the handle in handles BEFORE the factory runs; cancelAll() cannot actually cancel because the AbortController is stored only inside the closure — cancelAll writes to signal.reason but real abort happens via the (unshared) controller; cancelAll is effectively a no-op
- [HIGH] src/agent/worker-spawner.ts:82-94 - cancelAll's comment acknowledges it's a stub 'for the contract'; the real ctrl-C path in runtime/repl can't actually abort in-flight tool executions via this spawner
- [HIGH] src/agent/worker-spawner.ts:66-76 - factory promise rejects are propagated to done; but if factory calls 	hrow inside a synchronous executor before promise construction, the error is lost (not captured)
- [HIGH] src/agent/provider-adapter.ts:230-256 - req.signal.addEventListener with onAbort -> ac.abort(reason); the removal in finally uses the same handler ref so is correct; but when fetch fails before setting up reader the timer is NOT cleared — wait, it is cleared in the catch block
- [HIGH] src/agent/provider-adapter.ts:339-345 - for Ollama/vLLM providers that never send usage in SSE chunks, message_end is emitted WITHOUT usage; runtime's usage accounting silently underreports for local models
- [MED] src/agent/provider-adapter.ts:297-313 - toolIds map keyed by chunk.index; if OpenAI emits tool_calls with null index for the second call, deltas accumulate incorrectly
- [HIGH] src/agent/registry.ts:154-163 - buildProviderFromCli treats unknown provider names as openai with a stderr write; silent API key requirement: a user passing --provider anthropic but no key gets a clear error, but unknown names sail through and fail at first request with confusing HTTP 401
- [HIGH] src/agent/anthropic-adapter.ts:120-160 - 5xx -> retryable:true, 4xx (including 401) -> retryable:false, but 408/425/429 should be retryable; 4xx Anthropic overloaded errors are 'retryable:false' and stop the run
- [HIGH] src/agent/anthropic-adapter.ts:271-280 - error events drop parsed.error.message but do not surface AnthropicApiError class to caller; the runtime's ev.code becomes 'overloaded_error' etc; retry policy depends on retryable flag — fine, but caller has no recovery hook
- [MED] src/agent/anthropic-adapter.ts:284-320 - SSE event stream: state machine uses inToolUse + toolUseId stack; concurrent tool calls could interleave wrong input_json_delta if Anthropic emits them in mixed order (rare); current code keeps 'last started id' and routes by index — single-tool assumption
- [MED] src/agent/anthropic-adapter.ts:155 - 	ry { reader.releaseLock(); } only in finally; abort path may race with the message_end yield
- [MED] src/agent/anthropic-adapter.ts:90 - assertSafeBaseURL is imported from chat.ts; couples provider to chat; no equivalent guard for /v1/messages baseURL
- [HIGH] src/agent/provider-adapter.ts:296-305 - usage captured from chunk.usage but only emitted if 	oolIds.size > 0 or as last message_end; if the model never makes a tool call AND the response is a one-liner, usage can be lost (the else { yield {kind:'message_end'} } branch omits pendingUsage)
- [HIGH] src/agent/provider-adapter.ts:347-349 - 'else { yield message_end }' emits WITHOUT usage; pendingUsage is dropped silently. Tracked-cost underreports
- [MED] src/agent/provider-adapter.ts:300-339 - SSE parser uses buf.indexOf('\n\n') which splits on the SSE event boundary; a 'data:' line containing literal '\n\n' inside the JSON payload would split the event incorrectly (rare with JSON but possible)
- [HIGH] src/agent/retry.ts:90-130 - backoff is min(maxMs, baseMs * 2^attempt) ± jitter; jitter sign via Math.random()*2-1 but on each call the same attempt idx may compute different delays in different runs; non-determinism is fine but the delay = 0 short-circuit when attempt==maxAttempts-1 swallows a configured delay (intended)
- [HIGH] src/agent/retry.ts:107-122 - 'Buffer the retryable error; don't yield it yet' is implemented by reak then lastError = ev; but the consumer (runtime) only sees the *first* retryable event; if the adapter yields multiple retryable errors in one attempt only the first is preserved
- [MED] src/agent/retry.ts:99-105 - AbortSignal handling aborts the inner stream but does not abort the outer retry attempt; subsequent attempts re-issue the request
- [HIGH] src/agent/worker-spawner.ts:82-94 - cancelAll only sets signal.reason (try/catch) but does NOT call controller.abort(); factory promises continue; the documented contract 'cancel every active worker' is unimplemented — bug acknowledged in comment
- [HIGH] src/agent/worker-spawner.ts:67-76 - factory promise rejection propagates to handle.done but the handle stays in 	his.handles (delete is in .then/.catch) — race: if cancelAll runs while factory is in-flight, it iterates a Set that includes about-to-resolve handles
- [HIGH] src/agent/registry.ts:182-198 - buildProviderFromCli treats unknown provider strings as 'openai' with a stderr hint; security boundary: if --provider is from an untrusted source it silently falls through; not a vuln in CLI but the policy 'unknown provider -> openai default' is surprising
- [MED] src/agent/registry.ts:152-163 - 'anthropic requires --api-key or KLYRO_API_KEY' error fires only after providerName resolution; if user passes both --provider anthropic and empty --api-key the check fires correctly, but if KLYRO_API_KEY is empty string it passes (env trim) — likely fine
- [HIGH] src/chat.ts:75-95 - one-shot chat (legacy) does NOT use assertSafeBaseURL on KLYRO_BASE_URL unless baseURL is a string; CLI flag path skips the check; no SSRF guard for arbitrary user URLs
- [HIGH] src/index.ts:340-417 - session subcommands use a chain of wait per obs; or (const o of obs) process.stdout.write(...) is fine but the JSON.stringify of output.slice(0,120) may produce non-printable bytes for binary tool outputs (truncation in mid-UTF8)
- [MED] src/index.ts:574-575 - benchmark command runs unHarness([]) with empty args; should accept --suite <name> per design, currently a stub
- [LOW] src/index.ts:565-571 - hooks/agents/commit/audit commands are stubs; documented as 'Level 10.x' placeholders; not bugs per se but advertised in --help as real features
- [HIGH] src/agent/runtime.ts:602-604 - isConcurrencySafe !== false treats undefined as safe; default-true is the wrong safety choice for any tool that touches cwd/IO; only shell_exec, write_file, multi_edit, pply_patch, edit_file, memory_write set isConcurrencySafe:false but anything new defaults to parallel
- [LOW] src/agent/observation.ts - read; in-memory only, no persistence; flagged in module comment as 'MVP', plan.md Level 14
- [LOW] src/agent/message.ts - read; clean; no issues
- [LOW] src/tools/fs/list-dir.ts - read; small dir listing, no traversal protection beyond lstat symlink filter (relies on path-guard upstream); missing skip for hidden dot-dirs
- [MED] src/agent/runtime.ts:716-731 - branches are identical; the comment claims 'parallel execution previously pushed tool_results out of order' but both if and else are sequential or await — code smell suggesting a previous fix was incomplete and the dead llSafe variable should be removed
- [MED] src/agent/runtime.ts:651-653 - epairs++ incremented in approval 'edit' branch even when policy denies; conflates policy repairs with verification repair attempts in the final RunResult.repairs count
- [HIGH] src/tools/search/grep.ts:30-50 - ReDoS check is a regex .test() against a tiny set of patterns; an attacker can submit (a+)+$ which is not in the blocklist; combined with maxResults cap of 5000 the regex is run against all files
